<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.platform.warranty.mapper.RepairRecordMapper">

    <select id="selectRepairRecordCountOfType"
            resultType="com.platform.warranty.domain.vo.RepairRecordCountOfType">
SELECT
    rt.name AS repair_type_name,
    COUNT(rr.id) AS record_count
FROM
    repair_record rr
JOIN
    repair_type rt ON rr.type_id = rt.id
GROUP BY
    rt.name;

    </select>
</mapper>
